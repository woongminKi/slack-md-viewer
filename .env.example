# ============================================
# Slack Markdown Viewer - Environment Variables
# ============================================

# Slack App Credentials (Required)
# Get these from https://api.slack.com/apps > Your App > Basic Information
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret

# Socket Mode (Development)
# Get App Token from https://api.slack.com/apps > Your App > Basic Information > App-Level Tokens
SLACK_APP_TOKEN=xapp-your-app-token

# OAuth (Required for Public Distribution)
# Get these from https://api.slack.com/apps > Your App > OAuth & Permissions
SLACK_CLIENT_ID=your-client-id
SLACK_CLIENT_SECRET=your-client-secret

# ============================================
# Server Configuration
# ============================================

# Port (default: 3000)
PORT=3000

# Base URL - IMPORTANT: Update this for production!
# Development:
BASE_URL=http://localhost:3000
# Production (Railway example):
# BASE_URL=https://your-app-name.up.railway.app

# ============================================
# Storage Configuration
# ============================================

# TTL in milliseconds (default: 24 hours = 86400000)
STORAGE_TTL=86400000

# ============================================
# Redis Configuration (Recommended for Production)
# ============================================

# Redis connection URL
# Railway: Add Redis plugin, then use ${{Redis.REDIS_URL}} or copy the URL
# Local: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# ============================================
# Production Deployment Checklist
# ============================================
# 1. Set BASE_URL to your production URL (with HTTPS)
# 2. Set SLACK_CLIENT_ID and SLACK_CLIENT_SECRET for OAuth
# 3. Update OAuth Redirect URL in Slack App settings:
#    https://api.slack.com/apps > Your App > OAuth & Permissions
#    Redirect URL: https://your-domain.com/slack/oauth_redirect
# 4. Enable "Public Distribution" in Slack App settings:
#    https://api.slack.com/apps > Your App > Manage Distribution
